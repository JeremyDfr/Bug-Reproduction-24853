import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bugReproduction',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
